function leeProcesa (archivo) {	/* lee un archivo y devuelve un array formado por		las lineas que lo componen	*/		if (File(archivo).exists === true) {		var textoArchivo = File(archivo);		textoArchivo.open('r');		textoArchivo.encoding = 'BINARY';				var contenidoTexto = textoArchivo.read();		textoArchivo.close();				return String(contenidoTexto).split('\n');				} else {			return false;		}}