function scaleLinear (firstValueScale, lastValueScale, minim, maxim, val) {/* Scale down a range of numbers with a known min and max value	this is large based in the answer posted by irritate at:	http://tinyurl.com/k2vhslt (stackoverflow)		This function require three values:	minim	--->	the minimun value of a serie of values	maxim	--->	the maximun value of a serie of values	val	--->	the value to be scaled		The input val is part of the domain	The min...max form the output range		In our case we want to scale minim and maxim to a linear scale from 0 to 100 so that:	f(min) = a = 0		---> firstValueScale	f(max) = b = 100	---> lastValueScale			This process is called linear normalization		scaleLinear(0, 100, 0, 56, 0);	---> 0	scaleLinear(0, 100, 0, 56, 56); 	---> 100	*/		var scalingFactor = (lastValueScale - firstValueScale) / (maxim - minim);	return (scalingFactor * (val - minim)) + firstValueScale;	}