function extractMaxValue (datos) {	var totalDatos = datos.length;	var i = 0;	var topValues = [];	var rows = [];	for (; i < totalDatos; i = i + 1) {			// copy the line splited into an array of the i line inside of		// datos so that it can be sort without change it		rows = datos[i].split('\t').slice();			topValues.push (rows.sort(mayorAMenor)[0]); // add the higher value of each month into the array diasMasLluviosos		}	return topValues.sort(mayorAMenor)[0];}function extractMinValue (datos) {	var totalDatos = datos.length;	var i = 0;	var bottomValues= [];	var rows = [];	for (; i < totalDatos; i = i + 1) {			// copy the line splited into an array of the i line inside of		// datos so that it can be sort without change it		rows = datos[i].split('\t').slice();			bottomValues.push (rows.sort(menorAMayor)[0]); // add the lower value of each into the array diasMenosLluviosos	}	return bottomValues.sort(menorAMayor)[0];}